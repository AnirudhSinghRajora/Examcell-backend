spring.application.name=resultGen

# JWT Properties Removed
# ================================
# JWT Configuration
# ================================
# IMPORTANT: Replace with a strong, environment-specific secret key! 
# Generate a secure random key (e.g., using openssl rand -hex 32)
# jwt.secret.key=your-super-strong-and-long-secret-key-here-replace-it-now-512bits
#
# jwt.expiration.ms=86400000 # 24 hours in milliseconds
# jwt.expiration.ms=3600000 # 1 hour
# jwt.expiration.ms=60000   # 1 minute (for testing)

spring.datasource.url=jdbc:mysql://localhost:3306/examcell
spring.datasource.username=anirudh
spring.datasource.password=anirudh

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true